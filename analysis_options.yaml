include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**

linter:
  rules:
    # Flutter 3.47 flags the conventional ignored callback arguments
    # (_, __, ___) as informational only. Keep CI strict for real issues.
    unnecessary_underscores: false
